package Modelo.Vehiculos;

public class Automovil extends Vehiculos {
    // Atributos específicos de automóvil
    private int numPuertas;
    private String tipoCombustible; // "Gasolina", "Diesel", "Eléctrico"

    // Tarifa base por hora (se puede ajustar)
    private static final double TARIFA_BASE = 10000.0; // $10.000

    // Constructor completo (incluye los atributos de Vehiculos)
    public Automovil(String matricula, String marca, String modelo, String color,
                     int numPuertas, String tipoCombustible) {
        // Llama al constructor de Vehiculos con horarios nulos (se asignarán después)
        super(matricula, marca, modelo, color, null, null);
        this.numPuertas = numPuertas;
        this.tipoCombustible = tipoCombustible;
    }

    // Constructor simplificado (para cuando no se piden todos los datos)
    public Automovil(String matricula, String marca, String modelo) {
        this(matricula, marca, modelo, null, 4, "Gasolina");
    }

    // Getters y Setters
    public int getNumPuertas() {
        return numPuertas;
    }

    public void setNumPuertas(int numPuertas) {
        this.numPuertas = numPuertas;
    }

    public String getTipoCombustible() {
        return tipoCombustible;
    }

    public void setTipoCombustible(String tipoCombustible) {
        this.tipoCombustible = tipoCombustible;
    }

    // Implementación del método abstracto de Vehiculos
    @Override
    public double calcularTarifa() {
        // Factor adicional según características (opcional)
        double factor = 1.0;
        if (numPuertas > 4) {
            factor = 1.2;      // autos más grandes
        }
        if ("Diesel".equalsIgnoreCase(tipoCombustible)) {
            factor *= 1.1;
        }
        return TARIFA_BASE * factor;
    }
}
